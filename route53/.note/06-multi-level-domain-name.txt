It is possible to add a multiple dot "." record in a hosted zone

For example, in the hosted zone "example.com",
we can add a record named "aa.bb.cc.dd.ee.ff.example.com"

It is not possible to add duplicate domain names in a hosted zone