This keeps a note on zip files for lambda deployment

#!/bin/bash
rm -r dist
mkdir dist

cp -r src dist
pip3 install -t dist -r dev-requirements.txt
rm terraform/zip-file-name.zip

cd dist
chmod 755 *
zip -r ../terraform/zip-file-name.zip *

